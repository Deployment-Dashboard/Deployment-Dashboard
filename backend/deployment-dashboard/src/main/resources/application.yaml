spring:
  application:
    name: deployment-dashboard
  datasource:
    url: jdbc:h2:mem:test
    driver-class-name: org.h2.Driver
    username: test
    password: test
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
